### INSTRUKTIONER

- Kör "npm install" för att installera de dependencies som behövs för att starta projektet.
- Kör sedan "nodemon start" för att starta projektet.

- Notera example.env-filen. Där finns min mongodb uri & token. Skapa en .env fil med just det innehållet innan du startar upp projektet.
