### INSTRUKTIONER

- Kör "npm install" för att installera de dependencies som behövs för att starta projektet.
- Kör sedan "npm run dev -- --port 3001" för att starta projektet. (Eller valfri annan port, men back-end körs på port 3000, så inte den.)
